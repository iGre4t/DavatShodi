RewriteEngine On
RewriteBase /invite/

# Route requests to the controller under /invite/ so we can reuse the same generator.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.+)$ index.php/$1 [QSA,L]
