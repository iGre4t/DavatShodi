Options -Indexes

<IfModule mod_authz_core.c>
    <FilesMatch "\.(json|log)$">
        Require all denied
    </FilesMatch>
</IfModule>

<IfModule !mod_authz_core.c>
    <FilesMatch "\.(json|log)$">
        Order Allow,Deny
        Deny from all
    </FilesMatch>
</IfModule>
